[project]
name = "fastapi-project"
version = "0.1.0"
description = "FastAPI project to learn FastAPI framework"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.135.1",
]
